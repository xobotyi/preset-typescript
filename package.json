{
  "name": "@xobotyi/preset-typescript",
  "publishConfig": {
    "access": "public"
  },
  "version": "1.0.0",
  "peerDependencies": {
    "typescript": "^3.7"
  },
  "scripts": {
    "semantic-release": "semantic-release"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/xobotyi/preset-typescript.git"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^5.0.0",
    "@semantic-release/git": "^8.0.0",
    "@semantic-release/npm": "^7.0.3",
    "semantic-release": "^17.0.3"
  },
  "release": {
    "branches": [
      "master",
      {
        "name": "next",
        "prerelease": "rc"
      }
    ],
    "verifyConditions": [
      "@semantic-release/changelog",
      "@semantic-release/npm",
      "@semantic-release/git",
      "@semantic-release/github"
    ],
    "prepare": [
      "@semantic-release/changelog",
      "@semantic-release/npm",
      "@semantic-release/git"
    ]
  }
}
